import {
  "moonbitlang/core/argparse",
  "moonbitlang/async",
  "moonbitlang/async/stdio",
  "moonbitlang/x/fs",
  "moonbitlang/x/sys",
  "mik1e80/json2md",
}

// 读标准输入要走 moonbitlang/async，它只在 wasm / native 上有实现

supported_targets = "-all+wasm+native"

pkgtype(kind: "executable")
